# nanni_att bits 4 row matrix family level

architecture: nanni_att

level: family

coding: 4 row matrix

command used:

```
PYTHONPATH=$(pwd)/src python run_singlerank_experiment.py --config src/models/hyperparams/singlerank/att/nanni_att_family_bits0_hparams.json 
```
