# nanni_att 4 row matrix order level fast mode

architecture: nanni_att

level: order

coding: 4rowmatrix

command used:

```
PYTHONPATH=$(pwd)/src python run_singlerank_experiment.py --config src/models/hyperparams/singlerank/att/nanni_att_order_bits0_hparams.json
```
