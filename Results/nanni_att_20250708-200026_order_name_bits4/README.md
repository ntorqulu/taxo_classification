# nanni_att bits 4 order level

architecture: nanni_att

level: order

coding: bits 4

command used:

```
PYTHONPATH=$(pwd)/src python run_singlerank_experiment.py --config src/models/hyperparams/singlerank/att/nanni_att_order_bits4_hparams.json 
```
