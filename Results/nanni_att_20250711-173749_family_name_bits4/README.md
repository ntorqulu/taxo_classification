# nanni_att bits 4 family level fast mode batch_norm

architecture: nanni_att

level: family

coding: bits4

command used:

```
PYTHONPATH=$(pwd)/src python run_singlerank_experiment.py --config src/models/hyperparams/singlerank/att/nanni_att_family_bits4_hparams.json
```
